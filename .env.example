# Copiar a .env y completar con las claves reales (nunca subir el .env al repo)
# openai | anthropic
LLM_PROVIDER=openai

OPENAI_API_KEY=
ANTHROPIC_API_KEY=

# Opcional: sobreescribe el modelo por defecto del proveedor
# LLM_MODEL=gpt-4o-mini
